```bash
python commander2.py -p duoshuai -s ds -m view -a "forks=10"
python commander2.py -p duoshuai -s ds -m stop -a "forks=10"
python commander2.py -p duoshuai -s ds -m update -a "sync=yes zip=yes forks=10"
python commander2.py -p duoshuai -s ds -m check -a "forks=10"
python commander2.py -p duoshuai -s ds -m start -a "forks=10"
python commander2.py -p duoshuai -s ds -m view -a "forks=10"
```

## 停服操作记录

```bash
1、文件上传更新
2、ecs降配
✅️夺帅-php服-陈旭20240925	10.160.52.88 
✅️夺帅-模式信件服务-黄位森20240925	10.160.52.74
3、ecs下架
✅️夺帅-日志消费-porter-陈旭20240925	10.160.52.78
4、mysql下架
✅️rr-bp1p382t0135en6go	夺帅-游戏数据库-只读

未做的：观察俩天来下架
r-bp188es2ujjd9e5ve2	夺帅-二级缓存redis-陈言蒙
r-bp1nu8l5sweq1ww4jc	夺帅-排行榜redis
夺帅-cs-20000-ms-25020-ls-25021-25022-陈安20260704	ds-game-52-103	10.160.52.103
夺帅-ps-29101-29104	iZbp14z8oasjw10d6w3l05Z	10.160.52.112
```

## Todo List

```bash
1、游戏服：文件准备好了
✅️操作1：
10.160.52.103和10.160.52.113合并----》10.160.52.103下架
10.160.52.112和10.160.52.111合并----》10.160.52.112下架

❌️这次不做操作2：
k8s:先添加1台4C8G，排水1台8C16G,后期三台改为 4C8G
❌️这次不做操作3：ETCD降内存

✅️操作4：
php服启动命令 cat /etc/rc.local 
cd /data/www/web/m-way/update
sh /usr/local/node_exporter/start_node_exporter.sh

/usr/local/webserver/nginx/sbin/nginx
/usr/local/webserver/php5.5/sbin/php-fpm


验证 PHP-FPM 已经启动
ps aux | grep php-fpm
netstat -tlnp | grep 9000


✅️操作5：提前做了
夺帅-日志消费-porter-陈旭20240925-10.160.52.78 部署在 10.160.52.118，后这台下架掉
旧机器得定时任务也要迁移
0 * * * * /usr/bin/python /data/user_id_tool/user_id_tool.py >/data/user_id_tool/4.log 2>&1 &
0 * * * * /usr/bin/python /data/user_id_tool/acc_id_tool.py >/data/user_id_tool/5.log 2>&1 &

新机器上
0 * * * * /usr/bin/python3 /data/user_id_tool/user_id_tool.py >/data/user_id_tool/4.log 2>&1 &
修改python3用法
import importlib
importlib.reload(sys)
# sys.setdefaultencoding('utf-8')

0 * * * * /usr/bin/python3 /data/user_id_tool/acc_id_tool.py >/data/user_id_tool/5.log 2>&1 &



2、Redis合并
①不用同步数据-要操作下架
r-bp14yw51pgm8u3euzc	夺帅-游戏redis-陈旭20240925
r-bp188es2ujjd9e5ve2	夺帅-二级缓存redis-陈言蒙 下架掉
r-bp14yw51pgm8u3euzc.redis.rds.aliyuncs.com
新建账号：cache 
密码：Kqc^yqy41732

②要同步数据-要操作下架
r-bp14yw51pgm8u3euzc	夺帅-游戏redis-陈旭20240925
r-bp1nu8l5sweq1ww4jc	夺帅-排行榜redis 下架掉
r-bp14yw51pgm8u3euzc.redis.rds.aliyuncs.com
新建账号：rank 
密码：rnbN8mDg!KG0



3、MySQL合并
下架一台只读数据库
主库地址：rm-bp1575dxa5w263704.mysql.rds.aliyuncs.com
账号：msg
密码：ZMRPl3etc8S9Qj5E
```



## 1、修改对应的文件

```bash
pconf/netconfig.json
gameconfig/xmlconfig/ai_assign.xml
iniconfig/resource_config.ini
```

## 2、上传文件

```bash
python commander2.py -p duoshuai -s ds -m update -a "sync=yes zip=yes force=yes forks=10"


# 在服务器上检查文件是否一致
cat /data/sgs2-new/pconf/netconfig.json
```

## 3、生成supervisorctl服务对应的ini文件

```bash
# 先删除之后重新生成新的sh文件
python commander2.py -p duoshuai -s ds -m shell -a "rm -rf  /etc/supervisord.d/server.ini"
python commander2.py -p duoshuai -s ds -m shell -a "rm -rf  /tools/soldier2/bin/shell/*"

# 生成新的ini文件
python commander2.py -p duoshuai -s ds -m shell -a "/usr/bin/python3 /tools/soldier2/init/init_sh_ds.py "
python commander2.py -p duoshuai -s ds -m shell -a "supervisorctl update "
python commander2.py -p duoshuai -s ds -m reload
```

## 4、重启服务

```bash
python commander2.py -p duoshuai -s ds -m start -a "forks=10"
```



## 5、其他操作

```bash
拷贝可执行文件
# 起一个python http服务
cd /data/sgs2-new/gt
python3 -m http.server 8888
 

[root@ds-game gt]# wget -c  http://10.160.52.108:8888/Gateway 
[root@ds-game gt]# chmod 755 Gateway 
```

```bash
# 或者重新更新一个包进去
# 拷贝 gameconfig -生成的文件在工程common2的temp目录下，将gameconfig直接拷贝到update，然后去svn【publish_server】获取包文件，然后解压之后将server的内容拷贝到update

# 拉取到本地然后再上传
python commander2.py -p duoshuai -s 10.160.52.103 -m fetch -a "src=/data/sgs2-new/gameconfig/ dest=temp zip=no unzip=yes"
python commander2.py -p duoshuai -s new_up -m update -a "sync=yes zip=yes"
python commander2.py -p duoshuai -s new_up -m check -a "forks=10"
```



## porter迁移

```bash
旧机器上：
cd /data
tar  cf porter.tar.gz  porter/*
python3 -m http.server 8888

新机器上：
mkdir /data
cd /data
wget -c  http://10.160.52.78:8888/porter.tar.gz
tar  xf porter.tar.gz 

安装supervisor
dnf install -y epel-release
dnf install -y supervisor
systemctl enable --now  supervisord


旧机器上：
tar cf   supervisord.tar.gz   supervisord/*
python3 -m http.server 8888

新机器上：
tar xf supervisord.tar.gz 

ini里面的python环境需要修改一下/usr/bin/python3
supervisorctl reread
supervisorctl update


安装pip包
[root@ds-game etc]# /usr/local/python37/bin/python3 -m pip list
Package            Version
------------------ --------
certifi            2024.2.2
chardet            3.0.4
charset-normalizer 3.3.2
DBUtils            1.4
dnspython          2.3.0
filetype           1.1.0
idna               3.7
kafka-python       2.0.2
nacos-sdk-python   0.1.6
pip                23.3.1
psutil             5.9.6
pymongo            4.7.2
PyMySQL            0.9.3
PyYAML             5.3.1
redis              3.3.11
requests           2.31.0
setuptools         41.2.0
supervisor         4.2.4
urllib3            2.0.7
wget               3.2
zstandard          0.21.0
zstd               1.5.5.1


/usr/bin/python3 -m pip install kafka-python==2.0.2 DBUtils==1.4 redis pymysql
```

## etcd迁移方案

```bash

```

